const DIV_principal = ["overlay","]

function open(const DIV){
  for(let i = 0; i < DIV_principal.size()){
    
  }
  document.getElementById(DIV).style.display = "block";
  document.getElementById("overlay").style.display = "block";
}
